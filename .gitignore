node_modules/
.DS_Store
*.log

# Real Supabase project credentials — never commit. See utils/supabaseConfig.example.js.
utils/supabaseConfig.js

samples/

# Build output — the submission archive and its staging tree are generated by
# scripts/build-zip.mjs and must never be committed.
dist/
